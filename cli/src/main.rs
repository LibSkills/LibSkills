// Placeholder for `libskills` CLI.
// Planned features:
// - libskills search <keyword>    — fuzzy search against index.json
// - libskills get <path>          — download a skill to local cache
// - libskills find <intent>       — semantic / vector search
// - libskills update              — refresh index.json
// - libskills list                — show locally cached skills
fn main() {
    println!("LibSkills CLI — coming soon.");
}
