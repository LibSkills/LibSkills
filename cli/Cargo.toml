[package]
name = "libskills"
version = "0.1.0"
edition = "2021"
description = "CLI for LibSkills — a centralized skill registry for open-source libraries"
license = "Apache-2.0"
repository = "https://github.com/LibSkills/LibSkills"

[dependencies]
